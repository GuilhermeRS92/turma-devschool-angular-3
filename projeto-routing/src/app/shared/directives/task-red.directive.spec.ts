import { TaskRedDirective } from './task-red.directive';

describe('TaskRedDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskRedDirective();
    expect(directive).toBeTruthy();
  });
});
